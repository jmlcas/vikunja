# Vikunja

Antes de ejecutar el comando "docker-compose up -d"

Ejecutar el comando "chown 1000 ./files ./db

Ver en "http://localhost:8200"

Una vez ya registrado cambiar: VIKUNJA_SERVICE_ENABLEREGISTRATION: 0  (en vez de 1) para evitar nuevos registros.
